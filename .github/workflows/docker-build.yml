name: Build and push Track 2 image

on:
  push:
    branches: [main, master, experimental]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Determine Docker tag
        id: tag
        run: |
          if [[ "${{ github.ref_name }}" == "experimental" ]]; then
            echo "tag=experimental" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: pjmorales04
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: pjmorales04/amd-track2-agent-v2:${{ steps.tag.outputs.tag }}
          build-args: |
            FIREWORKS_API_KEY=${{ secrets.FIREWORKS_API_KEY }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
